[package]
name = "lez-events"
version = "0.1.0"
edition = "2021"

[dependencies]
borsh = { version = "1.6.0", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
risc0-zkvm = { workspace = true }
